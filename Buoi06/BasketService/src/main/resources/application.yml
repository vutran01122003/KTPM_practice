server:
  port: 8003
spring:
  application:
    name: basket-service
  datasource:
    username: root
    password: 123456a
    url: jdbc:mysql://localhost:3306/cart-service
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
